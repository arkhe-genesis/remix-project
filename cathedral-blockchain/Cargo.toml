[package]
name = "cathedral-blockchain"
version = "0.1.0"
edition = "2021"

[dependencies]
socket2 = { version = "0.4", features = ["all"] }

libc = "0.2"

[features]
default = ["std"]
std = []
ffi = []

[lib]
crate-type = ["cdylib", "rlib"]
